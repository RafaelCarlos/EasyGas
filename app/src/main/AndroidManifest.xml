<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codigo.rafael.easygas">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_easygas"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTheme">

        <!-- Ao utilizar o false, apenas essa aplicação poderá acessar esse IntentService -->
        <service
            android:name=".service.LocationIntentService"
            android:exported="false" />

        <activity android:name=".MainActivity" />
        <activity
            android:name=".ActivityTeste"
            android:label="@string/title_activity_teste" />
        <activity
            android:name=".user.LoginActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.CadastrarUserActivity" />
        <activity android:name=".user.EnderecoCadastroActivity" />
        <activity android:name=".EnderecoAddActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity
            android:name=".TesteLocationActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DistribuidorActivity"
            android:theme="@style/AppTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".PedidoMapsActivity"
            android:theme="@style/AppTheme"
            android:label="@string/title_activity_pedido_maps"></activity>
    </application>

</manifest>